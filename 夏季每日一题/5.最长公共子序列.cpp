#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
const int N = 1e6 + 5;
int a[N], b[N];
int main()
{
    int n;
    cin >> n;
    for (int i = 1; i < n;i++)
        scanf("%d", &a[i]);
    for (int i = 1; i < n;i++)
        scanf("%d", &b[i]);
        return 0;
}